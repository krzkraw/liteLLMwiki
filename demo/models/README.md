# Local Models

Put externally hosted Gemma `.litertlm` files here when needed for local runs.

Expected filenames:

```text
gemma-4-E2B-it-web.litertlm
gemma-4-E2B-it.litertlm
```

Actual model binaries, partial downloads, and split model chunks are ignored by
Git and must not be committed.
