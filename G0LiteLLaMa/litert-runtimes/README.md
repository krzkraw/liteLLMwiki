# Local LiteRT-LM runtimes

The interactive installers place repo-local `litert-lm` tool environments here
when you select a LiteRT runtime. Runtime folders and uv tool environments are
local artifacts and are ignored by Git.

